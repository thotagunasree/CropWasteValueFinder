# Custom Teachable Machine Model Directory

Drop your exported Google Teachable Machine image model files here:
1. `model.json`
2. `metadata.json`
3. `weights.bin`

The application will automatically detect and load these files. If weights are not present, the app smoothly uses the built-in Demo Classification Mode for Paddy Straw, Banana Stem, and Corn Stalk.
